{
  "name": "notification",
  "version": "1.0.1",
  "description": "",
  "main": "server.js",
  "scripts": {
    "start": "npm run db:migrate && node server.js",
    "dev": "DOTENV_CONFIG_PATH=.env.local npm run db:migrate && DOTENV_CONFIG_PATH=.env.local node -r dotenv/config server.js",
    "db:migrate": "node -r dotenv/config ./node_modules/.bin/prisma migrate deploy",
    "postinstall": "prisma generate",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "apn": "^2.2.0",
    "dotenv": "^17.2.3",
    "express": "^5.2.1",
    "express-session": "^1.17.3",
    "firebase-admin": "^13.6.0",
    "multer": "^1.4.5-lts.1",
    "prisma": "^5.22.0"
  }
}
